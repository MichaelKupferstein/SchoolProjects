package edu.yu.cs.com3810.skoop.user;

/**
 * The User class represents a user with a first name, last name, email, and university.
 */
public class User {

    private final String firstName;
    private final String lastName;
    private final String email;
    private final String university;

    /**
     * Constructs a new User with the specified first name, last name, email, and university.
     *
     * @param firstName the first name of the user
     * @param lastName the last name of the user
     * @param email the email address of the user
     * @param university the university the user is associated with
     */
    public User(String firstName, String lastName, String email, String university) {
        // Implementation needed
    }

    /**
     * Returns the first name of the user.
     *
     * @return the first name of the user
     */
    public String getFirstName() {
        // Implementation needed
    }

    /**
     * Returns the last name of the user.
     *
     * @return the last name of the user
     */
    public String getLastName() {
        // Implementation needed
    }

    /**
     * Returns the email address of the user.
     *
     * @return the email address of the user
     */
    public String getEmail() {
        // Implementation needed
    }

    /**
     * Returns the university the user is associated with.
     *
     * @return the university the user is associated with
     */
    public String getUniversity() {
        // Implementation needed
    }
}