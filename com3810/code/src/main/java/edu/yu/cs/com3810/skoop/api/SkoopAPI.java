package edu.yu.cs.com3810.skoop.api;

/**
 * Main API Application class that coordinates all components and handles client requests.
 */
public class SkoopAPI {

    /**
     * Initialize and start the Skoop API application.
     *
     * @param port the port number to listen on
     */
    public void start(int port) {
        //start the server on the given port
    }

    /**
     * Stop the Skoop API application gracefully.
     */
    public void shutdown() {

    }
}
